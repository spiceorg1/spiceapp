name: ewgenius.spice-app.runtime_metrics
type: append
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS "ewgenius"."spice-app".runtime_metrics (
        timestamp TIMESTAMP,
        metrics TEXT,
        value FLOAT
        labels TEXT
      )
